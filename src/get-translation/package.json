{
  "name": "get-translation",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "npx typescript -b -w",
		"compile": "npx rimraf dist && npx tsc -b"
  },
  "author": "Eric Johnson",
  "license": "MIT-0",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.13.1",
    "@aws-sdk/util-dynamodb": "^3.13.1"
  }
}
